# Inventory-Management-System-Using-Java
A simple console-based Inventory Management System written in Java that uses a `HashMap` to store items and their quantities.   The program allows users to add new items, restock existing items, sell items, check stock, display all items, and remove items.It provides menudriven interface and handles basic inventory operations with input validation.
